const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function init(){document.querySelector('#year').textContent=new Date().getFullYear();try{const r=await fetch('/api/site');if(!r.ok)throw 0;const d=await r.json();document.title=d.settings.site_title||document.title;heroTitle.textContent=d.settings.hero_title||heroTitle.textContent;heroText.textContent=d.settings.hero_text||heroText.textContent;if(d.message){message.classList.remove('hidden');message.innerHTML=`<strong>${esc(d.message.title)}</strong><span>${esc(d.message.body)}</span>`}days.innerHTML=d.days.length?d.days.map(x=>`<a class="day-card" href="/day.html?slug=${encodeURIComponent(x.slug)}" ${x.cover_key?`style="--cover:url('/api/media/${encodeURI(x.cover_key)}')"`:''}><div class="day-overlay"></div><div><span>${esc(x.date||'יום בקעמפ')}</span><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p><b>${x.media_count||0} פריטים ←</b></div></a>`).join(''):'<div class="empty">עוד מעט יעלו כאן הגלריות הראשונות ✨</div>'}catch{days.innerHTML='<div class="empty">לא הצלחנו לטעון כרגע. נסו לרענן.</div>'}}
subscribe.addEventListener('submit',async e=>{e.preventDefault();subStatus.textContent='שולח…';const body=Object.fromEntries(new FormData(e.target));const r=await fetch('/api/subscribe',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});subStatus.textContent=r.ok?'נרשמת בהצלחה! 🎉':'לא הצלחנו לרשום כרגע';if(r.ok)e.target.reset()});init();
